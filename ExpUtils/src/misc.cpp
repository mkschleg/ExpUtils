
#include "misc.h"


namespace ExpUtils{
namespace _misc{
  // std::vector<std::string>* stringExplode(std::string source, std::string delimiter){
  //   int found;
  //   std::vector<std::string>* results = new std::vector<std::string>;
  //   found=source.find_first_of(delimiter);
  //   while(found!=std::string::npos){
  //     if(found > 0){
  //       results->push_back(source.substr(0,found));
  //     }
  //     source = source.substr(found+1);
  //     found = source.find_first_of(delimiter);
  //   }
  //   if(source.length()>0) results->push_back(source);
  //   return results;
  // }
}
}
